import React from 'react'
import { SafeAreaView, StyleSheet, Text, View } from 'react-native'

const History = () => {
  return (
    <SafeAreaView style={{ padding: 8 ,flex: 1, justifyContent: 'center', alignItems: 'center'}}>
          <Text style={{fontSize:32,fontWeight:'600'}}>History</Text>
        </SafeAreaView>
  )
}

export default History

const styles = StyleSheet.create({})